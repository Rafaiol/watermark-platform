services:
  - type: web
    name: watermark-api
    env: docker
    dockerfilePath: ./backend/Dockerfile
    envVars:
      - key: PORT
        value: 8000
    plan: free # Note: free instances spin down after inactivity
